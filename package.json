[build]
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-run-functions"
  [plugins.inputs]
    functions = "functions"

[build.environment]
  # Variáveis de ambiente devem ser configuradas no Netlify UI para segurança
